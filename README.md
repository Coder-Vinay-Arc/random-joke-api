# Random Joke API Project 🎉

This is a beginner-friendly Python project that fetches a random joke using a public API.

## How it works
- Makes an API call
- Receives JSON response
- Displays joke in terminal

## Run the project
```bash
pip install -r requirements.txt
python app.py
## Features
- Fetches random jokes using public API
- Handles API/network errors
- Allows user to fetch multiple jokes interactively
